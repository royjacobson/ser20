"""
Bazel module definition for ser20 (cereal fork).
Modernized for Bazel 8 and bazelmod compatibility.
"""

module(
    name = "ser20",
    version = "0.9.0",
)

bazel_dep(name = "google_benchmark", version = "1.9.4")
bazel_dep(name = "rules_cc", version = "0.1.2")
